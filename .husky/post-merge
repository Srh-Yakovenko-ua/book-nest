if git diff --name-only HEAD@{1} HEAD 2>/dev/null | grep -q 'apps/api/prisma/schema.prisma'; then
  echo "[husky] prisma schema changed, regenerating client..."
  pnpm --filter @app/api db:generate
fi
